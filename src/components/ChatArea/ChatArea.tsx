'use client'

import React from 'react';

// Placeholder — full implementation (messages, MCQ, image upload, input) coming in a later step
export function ChatArea() {
  return (
    <div className="flex flex-col h-full bg-background text-white">
      <p className="text-text-secondary text-sm p-4">ChatArea placeholder</p>
    </div>
  );
}
