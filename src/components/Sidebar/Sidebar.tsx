'use client'

import React from 'react';

// Placeholder — full implementation (projects list, credit meter, nav) coming in a later step
export function Sidebar() {
  return (
    <nav className="h-full w-full bg-background text-white flex flex-col p-4">
      <p className="text-text-secondary text-sm">Sidebar placeholder</p>
    </nav>
  );
}
